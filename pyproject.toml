[project]
name = "comfyui-krea2-weightedconditioning"
description = "Krea2 Split Encode: person as a VAE frame, scene and clothing as Qwen placement; prompt mix plus Identity Edit without source latents."
version = "1.0.0"
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
classifiers = [
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]

[project.urls]
Repository = "https://github.com/cicalooo/ComfyUI-Krea2-WeightedConditioning"

[tool.comfy]
PublisherId = "cicalooo"
DisplayName = "Krea2 Split Encode"
Icon = ""
